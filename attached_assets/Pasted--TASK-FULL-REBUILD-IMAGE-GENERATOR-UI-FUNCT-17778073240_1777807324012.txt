--------------------------------------------------
TASK: FULL REBUILD — IMAGE GENERATOR (UI + FUNCTION + SYSTEM INTEGRATION)
--------------------------------------------------

GOAL:
Fix Image Generator page COMPLETELY so that:

✔ matches all other tools (same layout, spacing, colors)
✔ header + footer consistent
✔ no broken sections or blank spacing
✔ correct image generation (prompt fix)
✔ clean SaaS UI
✔ fully responsive
✔ integrated with site system

--------------------------------------------------
SECTION 1: FIX GLOBAL LAYOUT BREAK (CRITICAL)
--------------------------------------------------

ISSUE:
Page shows:
❌ huge empty space
❌ washed background
❌ broken layout alignment
❌ footer appearing incorrectly

--------------------------------------------------

FIX:

✔ wrap page in main container:

.main-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 40px 20px;
}

✔ remove any full-height forced sections

❌ remove:
height: 100vh;
min-height: 100vh;

✔ fix background:

body {
  background: #f8fafc;
}

--------------------------------------------------
SECTION 2: HEADER + FOOTER CONSISTENCY
--------------------------------------------------

Ensure:

✔ SAME header component used everywhere
✔ SAME footer component used everywhere

--------------------------------------------------

HEADER:

✔ sticky
✔ no extra spacing above
✔ correct z-index

header {
  position: sticky;
  top: 0;
  z-index: 1000;
}

--------------------------------------------------

FOOTER FIX:

Problem:
❌ washed green + low contrast text

Fix:

footer {
  background: #0f172a;
  color: #e2e8f0;
}

footer a {
  color: #cbd5f5;
}

--------------------------------------------------
SECTION 3: HERO SECTION FIX
--------------------------------------------------

Your current hero is too loose.

--------------------------------------------------

REBUILD HERO:

✔ center aligned
✔ tight spacing
✔ proper contrast

.hero {
  padding: 60px 20px;
  text-align: center;
}

.hero h1 {
  font-size: 42px;
  font-weight: 700;
  color: #0f172a;
}

.hero p {
  color: #475569;
  max-width: 600px;
  margin: 0 auto;
}

--------------------------------------------------
SECTION 4: TOOL LAYOUT FIX (IMPORTANT)
--------------------------------------------------

Current:
❌ misaligned cards
❌ uneven spacing

--------------------------------------------------

NEW STRUCTURE:

.grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px;
}

--------------------------------------------------

MOBILE:

@media (max-width: 768px) {
  .grid {
    grid-template-columns: 1fr;
  }
}

--------------------------------------------------
SECTION 5: CARD DESIGN FIX
--------------------------------------------------

.card {
  background: white;
  border-radius: 16px;
  padding: 20px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.05);
}

--------------------------------------------------
SECTION 6: IMAGE OUTPUT FIX
--------------------------------------------------

.generated-image img {
  width: 100%;
  border-radius: 12px;
  object-fit: cover;
}

--------------------------------------------------
SECTION 7: FIX IMAGE GENERATION LOGIC
--------------------------------------------------

Replace generation logic:

--------------------------------------------------

function enhancePrompt(input, style) {

  let text = input.toLowerCase();

  if (text.includes("masjid e nabwi") || text.includes("masjid nabwi")) {
    text = "Al-Masjid an-Nabawi in Medina Saudi Arabia, green dome mosque, Islamic architecture, detailed interior";
  }

  const styleMap = {
    realistic: "photorealistic, natural lighting",
    cinematic: "cinematic lighting, dramatic shadows",
    anime: "anime style",
    "3d": "3D render"
  };

  return `${text}, ${styleMap[style] || ""}, ultra detailed, high quality, sharp focus`;
}

--------------------------------------------------

USE:

const finalPrompt = enhancePrompt(userInput, style);

--------------------------------------------------
SECTION 8: API CALL FIX
--------------------------------------------------

const url = `https://image.pollinations.ai/prompt/${encodeURIComponent(finalPrompt)}?width=${width}&height=${height}`;

--------------------------------------------------
SECTION 9: LOADING + ERROR UX
--------------------------------------------------

Before request:

✔ disable button
✔ show loader

--------------------------------------------------

On error:

Show:

"Image couldn't be generated. Try a more detailed prompt."

--------------------------------------------------
SECTION 10: REMOVE EMPTY SPACE BUG
--------------------------------------------------

CHECK:

❌ sections with huge padding
❌ duplicate containers
❌ wrong flex settings

--------------------------------------------------

Fix:

section {
  margin-bottom: 40px;
}

--------------------------------------------------
SECTION 11: INTERNAL LINKING
--------------------------------------------------

Add below tool:

✔ AI Voice Generator
✔ BMI Calculator
✔ Calorie Calculator

--------------------------------------------------
SECTION 12: FINAL VISUAL RESULT
--------------------------------------------------

After fix:

✔ no blank green area
✔ consistent header/footer
✔ clean centered layout
✔ working generator
✔ proper spacing
✔ same feel as other tools

--------------------------------------------------
CONFIRMATION
--------------------------------------------------

"Image generator fully fixed with consistent UI, layout, and working generation system"