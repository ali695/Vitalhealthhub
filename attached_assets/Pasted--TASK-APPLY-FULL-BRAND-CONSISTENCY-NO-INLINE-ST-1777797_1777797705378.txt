--------------------------------------------------
TASK: APPLY FULL BRAND CONSISTENCY (NO INLINE STYLES)
--------------------------------------------------

GOAL:
Transform the entire website into a CLEAN, SCALABLE, and PROFESSIONAL design system.

Everything MUST use:
✔ Global CSS classes
✔ Reusable components
✔ Consistent variables

ABSOLUTE RULE:
❌ NO inline styles anywhere
❌ NO style="" attributes
❌ NO per-page styling
✔ ALL styling must come from global CSS system

--------------------------------------------------
SECTION 0: REMOVE INLINE STYLES (CRITICAL)
--------------------------------------------------

Scan entire codebase:

REMOVE all inline styles like:
style="..."
style="position:relative"
style="z-index:2"

--------------------------------------------------

REPLACE with classes:

Example:

❌ WRONG:
<div style="position:relative;z-index:2;">

✔ CORRECT:
<div class="relative-layer">

--------------------------------------------------

DEFINE in CSS:

.relative-layer {
  position: relative;
  z-index: 2;
}

--------------------------------------------------
SECTION 1: GLOBAL CSS VARIABLES (ROOT SYSTEM)
--------------------------------------------------

Create global variables:

:root {
  --vh-green: #22C55E;
  --vh-green-dark: #16A34A;
  --vh-dark: #052e1f;
  --vh-dark-2: #064e3b;
  --vh-bg-light: #ECFDF5;
  --vh-white: #FFFFFF;

  --vh-text-primary: #111827;
  --vh-text-secondary: #374151;
  --vh-text-muted: #6B7280;

  --vh-radius-card: 16px;
  --vh-radius-btn: 999px;

  --vh-shadow-1: 0 4px 12px rgba(0,0,0,0.04), 0 1px 3px rgba(0,0,0,0.06);
  --vh-shadow-2: 0 10px 25px rgba(0,0,0,0.08), 0 4px 10px rgba(0,0,0,0.06);
  --vh-shadow-3: 0 10px 30px rgba(0,0,0,0.15);
}

--------------------------------------------------

RULE:

✔ Use ONLY variables
❌ No direct hex values in components

--------------------------------------------------
SECTION 2: GLOBAL LAYOUT CLASSES
--------------------------------------------------

Create reusable layout classes:

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

.section {
  padding: 60px 0;
}

.section-sm {
  padding: 40px 0;
}

.text-center {
  text-align: center;
}

--------------------------------------------------

RULE:

✔ No custom spacing per section
✔ Only use defined classes

--------------------------------------------------
SECTION 3: TYPOGRAPHY CLASSES
--------------------------------------------------

Create reusable text system:

.heading-xl {
  font-weight: 700;
  line-height: 1.2;
  letter-spacing: -0.02em;
}

.text-primary {
  color: var(--vh-text-primary);
}

.text-secondary {
  color: var(--vh-text-secondary);
}

.text-muted {
  color: var(--vh-text-muted);
}

--------------------------------------------------

RULE:

❌ No inline font-size or colors
✔ Always use classes

--------------------------------------------------
SECTION 4: BUTTON COMPONENT SYSTEM
--------------------------------------------------

Create reusable buttons:

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--vh-radius-btn);
  font-weight: 600;
  transition: all 0.25s ease;
}

.btn-primary {
  background: var(--vh-green);
  color: #fff;
  box-shadow: 0 6px 18px rgba(34,197,94,0.35);
}

.btn-primary:hover {
  background: var(--vh-green-dark);
  transform: translateY(-2px);
}

.btn-outline {
  border: 1px solid var(--vh-green);
  color: var(--vh-green);
  background: transparent;
}

--------------------------------------------------

RULE:

❌ No custom button styles per page
✔ Only reuse .btn classes

--------------------------------------------------
SECTION 5: CARD COMPONENT SYSTEM
--------------------------------------------------

Create unified card class:

.card {
  background: var(--vh-white);
  border-radius: var(--vh-radius-card);
  padding: 24px;
  box-shadow: var(--vh-shadow-1);
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--vh-shadow-2);
}

--------------------------------------------------

APPLY TO:

✔ calculators
✔ tools
✔ blog cards
✔ quizzes

--------------------------------------------------
SECTION 6: ICON SYSTEM (NO EMOJIS)
--------------------------------------------------

Replace ALL emojis:

❌ REMOVE:
🔥 😴 🌈 🧠 💧

✔ USE:
SVG icons only

--------------------------------------------------

RULE:

✔ same stroke width
✔ same style across site

--------------------------------------------------
SECTION 7: BLOG CTA SYSTEM
--------------------------------------------------

Create reusable class:

.blog-cta {
  background: linear-gradient(135deg, var(--vh-dark), var(--vh-dark-2));
  border-radius: 16px;
  padding: 40px;
  text-align: center;
}

.blog-cta h3 {
  color: #ECFDF5;
}

.blog-cta p {
  color: #A7F3D0;
}

--------------------------------------------------

RULE:

❌ No custom CTA per page
✔ Use this component everywhere

--------------------------------------------------
SECTION 8: TABLE SYSTEM
--------------------------------------------------

Create reusable table class:

.table-dark {
  background: var(--vh-dark);
  color: #ECFDF5;
  border-radius: 16px;
  overflow: hidden;
}

.table-dark tr:nth-child(even) {
  background: var(--vh-dark-2);
}

.table-dark tr:hover {
  background: #065f46;
}

--------------------------------------------------
SECTION 9: SPACING SYSTEM
--------------------------------------------------

Define spacing utilities:

.mt-16 { margin-top: 16px; }
.mt-24 { margin-top: 24px; }
.mt-40 { margin-top: 40px; }

--------------------------------------------------

RULE:

❌ No random margin/padding values
✔ Only use system spacing

--------------------------------------------------
SECTION 10: REMOVE DESIGN INCONSISTENCIES
--------------------------------------------------

Fix across ALL pages:

✔ same shadows
✔ same radius
✔ same buttons
✔ same spacing
✔ same font scale

--------------------------------------------------

REMOVE:

❌ random UI differences
❌ mixed styling patterns
❌ inconsistent components

--------------------------------------------------
SECTION 11: BLOG IMAGE SYSTEM
--------------------------------------------------

Ensure:

✔ all blogs have featured images
✔ consistent ratio (1200x630)
✔ optimized images
✔ no duplicates

--------------------------------------------------
SECTION 12: FINAL CLEANUP
--------------------------------------------------

Scan entire site:

✔ remove inline styles
✔ replace with classes
✔ unify all components
✔ enforce variables

--------------------------------------------------
FINAL OUTPUT
--------------------------------------------------

✔ Clean scalable CSS system
✔ No inline styling
✔ Reusable components
✔ Consistent SaaS-level UI
✔ Professional brand identity

--------------------------------------------------
CONFIRMATION
--------------------------------------------------

"Global brand system applied with zero inline styles and full consistency across VitalHealth Hub"