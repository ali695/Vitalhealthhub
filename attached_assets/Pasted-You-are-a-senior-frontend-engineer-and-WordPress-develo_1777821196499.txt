You are a senior frontend engineer and WordPress developer specializing in responsive design, mobile UX, and performance optimization.

Your task is to audit and FIX an existing WordPress website that currently has major issues with responsiveness, layout, and interaction across devices.

⚠️ IMPORTANT:
Do NOT rebuild everything randomly.
You must FIX the current system properly using clean CSS, proper layout structure, and correct JavaScript behavior.

---

🎯 PRIMARY GOAL:
Make the entire website FULLY RESPONSIVE and FULLY FUNCTIONAL across ALL DEVICES:

* Mobile (320px – 768px)
* Tablet (768px – 1024px)
* Laptop (1024px – 1440px)
* Desktop (1440px+)

---

🚨 CURRENT ISSUES TO FIX:

1. Buttons are not clickable on mobile
2. Chatbot is blocking UI interactions
3. Mobile navigation menu is broken or hard to open
4. Quiz pages are not working on mobile
5. Blog pages are not responsive
6. Layout is broken on smaller screens
7. Elements overlap each other
8. Some sections are not scrollable or clickable
9. Fixed widths causing overflow
10. Navigation + chatbot not clickable on quiz page

---

🧠 YOUR TASK:

You must scan ALL:

* Pages
* Blog templates
* Quiz pages
* Calculators
* Navigation
* Header & Footer
* Buttons & CTAs
* Chatbot component

Then FIX EVERYTHING.

---

📱 RESPONSIVENESS FIX REQUIREMENTS:

Apply a GLOBAL RESPONSIVE SYSTEM:

* Use fluid layouts (no fixed widths)

* Use flexbox and CSS grid properly

* Add proper breakpoints:

  * 320px
  * 480px
  * 768px
  * 1024px
  * 1440px

* Ensure:

  * No horizontal scroll
  * No overflow issues
  * Proper spacing
  * Readable font sizes
  * Mobile-first scaling

---

🔘 BUTTON & CLICK FIX:

Fix all click issues:

* Ensure buttons are clickable on all devices

* Fix z-index conflicts

* Remove overlay blocking clicks

* Add:
  pointer-events: auto where needed
  pointer-events: none for blocking overlays

* Ensure:

  * CTA buttons always clickable
  * Navigation always clickable
  * Quiz buttons work on touch

---

📲 MOBILE NAVIGATION FIX:

Fix navigation completely:

* Create a working hamburger menu
* Smooth open/close animation
* Add overlay background
* Prevent background scroll when menu is open
* Ensure menu works on touch devices
* Ensure menu closes properly

---

🤖 CHATBOT FIX:

* Fix chatbot overlay blocking UI

* Ensure it does NOT block clicks

* Proper z-index layering:
  chatbot < navigation < modals

* Make chatbot responsive:

  * Smaller on mobile
  * Expandable
  * Not covering buttons

---

🧪 QUIZ & CALCULATOR FIX:

* Fix all JavaScript interaction issues
* Ensure:

  * Buttons respond to touch
  * No overlap blocking clicks
  * Inputs are usable
  * Forms work properly
  * Navigation still clickable

---

📰 BLOG PAGE FIX:

* Fix blog layout:

  * Responsive images
  * Proper container width
  * Readable typography
  * No overflow

* Ensure:

  * Featured images scale properly
  * Text wraps correctly
  * Internal links clickable

---

🎨 CSS FIX STRATEGY:

* Refactor CSS:

  * Remove fixed widths
  * Replace with max-width + flex/grid
  * Add responsive units (%, rem, vw)

* Add:

  * global container system
  * responsive typography
  * spacing system

---

⚙️ JS FIX STRATEGY:

* Fix event listeners for:

  * click
  * touchstart
  * touchend

* Prevent:

  * event blocking
  * propagation issues

---

🔍 TESTING (VERY IMPORTANT):

Simulate and test on:

* iPhone SE (small screen)
* iPhone 14 Pro
* Android devices
* Tablets
* Laptops
* Large desktops

Test:

* Navigation
* Buttons
* Forms
* Quiz
* Calculators
* Scrolling
* Click interactions

---

🚀 FINAL RESULT:

After your fixes:

* All pages must be fully responsive
* All buttons must be clickable
* Navigation must work perfectly
* Chatbot must not block UI
* Quiz must work on mobile
* Blog pages must be clean and readable
* No layout breaks on any device
* No overlapping elements
* No horizontal scroll

---

⚠️ IMPORTANT RULES:

* Do NOT use quick hacks
* Do NOT hide elements to fix layout
* Do NOT break desktop while fixing mobile
* Keep performance optimized
* Keep code clean and production-ready

---

🎯 OUTPUT:

Provide:

* Updated CSS files
* Updated JS files
* Any necessary HTML fixes

Everything must be ready to deploy in WordPress theme/plugin.

Act like a senior engineer fixing a production site — not a beginner applying patches.
